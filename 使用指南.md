# Wordbook 应用使用指南

## 一、应用简介

Wordbook 是一款功能强大的单词学习应用，通过 **DeepSeek AI 智能翻译** + 本地离线词库，帮助用户通过阅读文章、收集单词、卡片式学习等方式高效记忆英语单词。AI 翻译会返回单词的**词性 + 中文释义**，比传统机翻更加准确。

## 二、UI 结构

```
📱 Wordbook 单词本
│
├── 📖 词组页面（首页）
│   ├── 标题栏: "我的词组" + [添加词组] 按钮
│   └── 词组卡片列表 —— 按文章分组展示
│
├── 📝 我的单词页面
│   ├── 标题栏: "我的单词" + 卡片数量 + [添加单词] 按钮
│   └── 单词列表 —— 所有已收录的单词
│
├── ⚙️ 设置页面
│   └── DeepSeek AI 翻译配置
│       ├── API Key 输入框（密码模式）
│       ├── [保存配置] [清除配置] 按钮
│       └── 获取密钥的帮助说明
│
├── 📄 文章详情（学习页面）
│   ├── [← 返回] 按钮 + 词组标题
│   ├── 🃏 单词闪卡（点击翻转：单词 ↔ 释义）
│   ├── [上一个] [下一个] 导航按钮
│   └── 📃 原文展示区（当前单词高亮显示）
│
└── 🔽 底部导航栏
    ├── 📚 词组 —— 回到词组列表
    ├── 📄 单词 —— 查看所有单词
    └── ⚙️ 设置 —— API 配置
```

### 弹窗说明

| 弹窗 | 触发方式 | 功能 |
|------|---------|------|
| 添加选项弹窗 | 点击"添加词组" | 选择"添加文章"或"添加单词" |
| 粘贴文章弹窗 | 选择"添加文章" | 粘贴英文文章，点击选词 |
| 词组标题弹窗 | 选完单词保存时 | 输入词组名称 |
| 单词弹窗 | 点击"添加单词" | 手动输入单词和释义 |

## 三、主要功能

### 1. 文章阅读与单词收集
- 输入英文文章，系统会自动分词并标记可点击的单词
- 点击单词选中（再次点击取消），选中的单词会高亮
- 保存后系统自动调用 DeepSeek AI 翻译，返回**词性 + 释义**

### 2. 单词管理
- 查看已收集的单词列表，每张卡片显示单词和释义
- 长按单词卡片可删除单词
- 支持手动添加自定义单词和释义

### 3. 词组管理
- 将相关单词按文章组织成词组
- 长按词组卡片可删除整个词组
- 词组内单词按随机顺序展示（Fisher-Yates 洗牌）

### 4. 卡片式学习
- 点击词组进入卡片学习模式
- 卡片正面显示单词，**点击卡片翻转**查看释义
- 释义包含词性和中文解释（如：`n. | v. ①书，书籍 ②预订，预约`）
- 使用「上一个」和「下一个」按钮导航
- 原文区域高亮显示当前单词的位置

### 5. DeepSeek AI 翻译
- 优先使用 AI 翻译，获取带词性的准确释义
- AI 翻译失败时自动回退到本地词库（~5500 考研词汇）
- 两阶段都失败时显示提示文本

## 四、详细使用步骤

### 1. 首次使用 —— 配置 API
1. 前往 [DeepSeek 开放平台](https://platform.deepseek.com/) 注册账号
2. 进入 API Keys 页面，创建一个新的 API Key
3. 打开应用，点击底部「设置」tab
4. 将 API Key 粘贴到输入框，点击「保存配置」
5. 看到 "✓ DeepSeek API 已配置" 即表示成功

### 2. 添加词组（从文章提取单词）
1. 在首页点击「添加词组」按钮
2. 选择「添加文章」
3. 将英文文章粘贴到文本框中，点击「处理文章」
4. 系统自动分词并展示所有单词
5. **点击单词**选中需要学习的词汇（再次点击取消）
6. 点击「保存文章和单词」
7. 输入词组标题，点击「保存标题」
8. 系统自动翻译单词 → 返回词组列表

### 3. 手动添加单词
1. 在首页点击「添加词组」
2. 选择「添加单词」
3. 输入单词 → 系统自动翻译获取释义
4. 可手动修改释义，点击「保存单词」
5. 可继续添加下一个，或点击「结束添加」

### 4. 学习单词（闪卡模式）
1. 在首页点击任意词组卡片
2. 进入闪卡学习模式，显示单词
3. **点击卡片**查看释义（再次点击翻回）
4. 使用「上一个」和「下一个」按钮切换
5. 原文区域实时高亮当前单词
6. 点击左上角 ← 返回词组列表

### 5. 删除单词 / 词组
1. 在单词列表或词组列表中
2. **长按**要删除的卡片
3. 在弹出的确认对话框中点击「确认」

## 五、常见问题与解决方案

### 1. DeepSeek API 配置失败
- 检查 API Key 是否正确（以 `sk-` 开头）
- 确保网络连接正常
- 检查 DeepSeek 账户是否有余额
- 如仍失败，应用会自动使用本地词库

### 2. 翻译结果不理想
- AI 翻译基于 DeepSeek 大模型，大多数情况下比传统机翻准确
- 如果释义有误，可以长按单词手动修改
- 本地词库覆盖约 5500 常见考研词汇

### 3. 单词卡片显示异常
- 检查单词是否已正确添加
- 尝试重新进入词组学习模式

### 4. 应用崩溃或无响应
- 尝试重启应用
- 清除应用数据后重新配置 API Key

## 六、使用小贴士

1. **高效学习**: 每天学习 10-15 个单词，保持学习连续性
2. **分类管理**: 将单词按主题或文章来源分类到不同词组
3. **复习计划**: 定期复习已学单词，巩固记忆
4. **语境学习**: 通过阅读文章学习单词，理解单词在真实语境中的用法
5. **利用碎片时间**: 在通勤、等待等碎片时间使用卡片学习模式
6. **AI + 本地双重保障**: 有网时用 AI 获取精准释义，无网时本地词库兜底

---

**版本**: 1.2.0
**更新日期**: 2026-07-29
