# 需求文档：语境学习伴侣（"问它"）

| 项 | 值 |
|---|---|
| 版本 | v0.1 |
| 日期 | 2026-08-13 |
| 状态 | 评审中 |
| 所属方向 | 方向 3：语境学习伴侣（体验升级） |
| 产品形态 | 标准 Agent：多轮对话 + 工具调用 + 反馈闭环（判定依据：笔记 Q1/Q8） |

---

## 1. 背景与目标

### 1.1 背景

Wordbook 为 WebView 架构考研英语应用（逻辑在 `app/src/main/assets/`，AI 通过 DeepSeek API 直连，现有单轮能力：单词释义、单词+句子翻译、长难句分析）。

现状痛点：复习卡片上用户看到原句却"只知其译、不知其所以然"——为什么要用这个词、这个词在这里是哪个义项、和近义词差在哪，现有功能回答不了。

### 1.2 目标

在闪卡复习中加入"问它"入口。用户可就**当前卡片**的词/句提出开放问题，AI 结合卡片语境 + 本地语法知识库回答，形成"看懂语境 → 理解用法 → 沉淀笔记"的学习闭环。

### 1.3 非目标（明确不做）

- 不做通用聊天机器人：问答范围限定在**当前卡片语境**，不做与学习无关的自由对话。
- 不做无界 agent 循环：每次提问最多 **2 轮** LLM 调用，硬上限。
- 不做热更新知识库：语法资料随 APK 静态打包，更新靠"改笔记 → 重跑导出脚本 → 重新打包"。
- 不做多语言/口语/写作等衍生功能，首版仅"英→中、考研口径"。

---

## 2. 用户场景

**S1 语境词义（主场景）**：复习时翻卡，看到原句 "…if the industry cannot **collect** information about their preferences"，点"问它"→"collect 在这里什么意思？"→ AI 给出该语境义项（收集、搜集）+ 画面感 + 同义替换。

**S2 一词多义**：追问"它还有其他意思吗？"→ AI 列出该词其他义项（收取、领取、募集、聚集），标注哪个义项适用于当前句。

**S3 母语者语感**：追问"母语者怎么理解这个词？"→ AI 用画面/意象解释母语者的直觉感受（collect = 主动去把散落的东西聚拢）。

**S4 近义词辨析**：追问"区分 collect 和 receive 的画面差异"→ AI 给出画面对比（collect 你去找它，receive 它来找你）+ 各自适用场景 + 对照例句。

**S5 用法/形式解释**：追问"为什么这里是过去分词/为什么用 which 不用 that"→ 结合语法知识库按考研口径解释。

**S6 无 API key / 断网**：仍可打开面板，提示配置密钥；或给出基于本地词库+原句翻译的离线模板回答。

---

## 3. 功能需求

### FR-1 "问它"入口

- **位置**：复习卡片翻面后，原句/翻译区（`review-sentence-section`）下方，"问它：这个词为什么这么用？"按钮。
- **可见性**：卡片有句子语境时始终可见；无句子时仍可用（降级为纯词义/辨析类问答），按钮文案不变。
- 复习完成面板不提供入口（复习流结束即清理）。

### FR-2 提问面板

- 形态：底部弹出式面板（半屏），不打断复习流程；关闭后回到卡片。
- 组成：
  - 快捷问题 chips（点击即问，用户可自定义）：
    - "这个词在这里为什么这么用？"
    - "它还有其他意思吗？"
    - "母语者怎么理解这个词？"
  - 自由输入框（支持"区分 X 和 Y"这类双词提问）；
  - 消息列表（用户问题 / AI 回答，AI 回答含"存为笔记"按钮）；
  - 加载态："AI 思考中…"（带 15s 超时兜底）。
- 回答渲染：mini markdown（`**加粗**`、换行、`-` 列表、`|` 表格），实现风格参照现有 `renderBreakdownToHtml`。

### FR-3 支持的提问类型（核心能力）

| 编号 | 类型 | 示例 | 回答要点 |
|---|---|---|---|
| T1 | 语境词义 | "collect 在这里什么意思？" | 语境义项 + 画面感 + 同义替换 |
| T2 | 一词多义 | "它还有其他意思吗？" | 义项列表 + 与本句的关联 |
| T3 | 母语者语感 | "母语者怎么理解这个词？" | 意象/画面 + 使用直觉 |
| T4 | 近义词辨析 | "区分 collect 和 receive 的画面差异" | 画面对比 + 适用场景 + 对照例句 |
| T5 | 用法/形式 | "为什么这里是这个形式/搭配？" | 规则依据 + 例句（与长难句分析同口径） |
| T6 | 其他开放问题 | 任意关于当前词/句的问题 | 尽力回答，约束在卡片语境内 |

### FR-4 Agent 引擎

- **有界循环**：每问最多 2 轮 LLM 调用。system prompt 硬约束"最多调用一次工具，下一轮必须直接回答"；代码层 `maxRounds = 2` 兜底。
- **工具协议**：模型输出 `{"tool": "...", "args": [...]}` JSON 决策，应用执行本地工具后回填结果，再进下一轮。
- **会话管理**：按卡片保留 `history`（追问共享上下文）；切卡片清空；退出复习清空；不持久化（可选 localStorage 缓存最近一次，见 FR-8）。
- **失败处理**：工具执行失败 → 错误信息回填给模型解释，或直接提示用户；**绝不自动重试**。

### FR-5 工具集

| 工具 | 实现 | 成本 | 说明 |
|---|---|---|---|
| `get_card_context` | 内存读取 `card.sentence / sentenceTranslation / meaning` | 免费 | 预填进首轮 prompt，不占用工具轮数 |
| `grammar_lookup` | 本地语法库检索（关键词分类） | 免费 | 知识源 = Obsidian 库导出，命中章节注入，长度封顶 |
| `offline_fallback` | 无 API key 时的模板拼装 | 免费 | 本地释义 + 原句翻译 + 固定模板，不走 LLM |

> **决策记录**：本地词典（`dictionary.js` 考研 5500 词）**不作为 agent 工具**——模型对该词的认知强于扁平释义表，且精确匹配命中率低；仅保留在应用现有翻译管线与离线降级中。

### FR-6 回答规范

- 结构（T1-T4 通用骨架）：**语境义项 → 依据/画面感 → 1-2 个例句**（英+中）→ 辨析题追加对比。
- 口径：语法术语、从句判定规则与现有长难句分析（`translator.js` analyzeSentence）**完全一致**。
- 长度：每轮 `max_tokens ≤ 800`，回答精炼，不铺陈。
- 内容红线：不编造词义；拿不准的义项标注"此义项较少见"；对比类回答必须给出双方适用场景，不能只夸一方。

### FR-7 反馈闭环

1. **追问**：同卡片多轮追问共享上下文，AI 能引用之前的回答（"刚才说的那个义项…"）。

2. **一致性**：回答口径与长难句分析功能互不矛盾。

### FR-8 成本控制

- 每次提问：LLM 调用 ≤ 2 次；`temperature 0.3`；`max_tokens ≤ 800`。
- 语法上下文按需注入：命中 ≤ 3 节 + 总览，`buildGrammarContext()` 长度封顶（约 3-6K token 输入/问）。
- 免费信息（卡片语境、命中语法节）一律预填首轮，典型路径 1 次调用出答案。
- 不做失败重试；不做模型主动循环。
- 可选：回答按"卡片 + 问题前缀"缓存于 localStorage，重复提问零成本。

### FR-9 降级与容错

- 无 API key：面板提示配置（跳转设置页）；或直接给出离线模板回答（FR-5 `offline_fallback`）。
- 超时：15s AbortController，提示"请求超时，请重试"。
- 错误映射：401 → 密钥无效；402 → 余额不足；其余 → 通用失败提示（复用 `translator.js` 错误处理模式）。
- 网络失败：面板保持打开，不丢已发问题，可重发。

---

## 4. 非功能需求

| 项 | 要求 |
|---|---|
| 性能 | 面板开合无卡顿；首版回答整段返回（不做流式）；渲染 ≤ 50ms |
| 体积 | `grammar.js` ≤ 120KB |
| 兼容 | minSdk 24；WebView 行为与现有功能一致 |
| 隐私 | 无新增权限；请求仍仅发往 `api.deepseek.com`；问题内容不上传任何其他服务 |
| 可维护 | 语法知识源（Obsidian 库）与产物（grammar.js）分离，导出脚本可复跑 |

---

## 5. 技术架构

```
C:\Users\jpc79\Documents\obsidian\英语\（知识源：词法/语法/句法 24 文件）
        │ tools/export_grammar.js（Node 脚本：清洗 frontmatter/wikilink → 分节 → 生成）
        ▼
app/src/main/assets/grammar.js（GRAMMAR_SECTIONS + buildGrammarContext 长度封顶）
        │ matchGrammarSections(question) 本地关键词分类（免费）
        ▼
assets/askAgent.js（新模块：有界循环 + JSON 工具协议 + 会话管理）
        │
        ▼
index.html 复习区"问它"按钮 + 提问面板 DOM  →  app.js 接入（渲染/事件/存笔记）
```

模块清单：

| 模块 | 文件 | 职责 |
|---|---|---|
| 知识库 | `assets/grammar.js`（生成物） | 语法章节库 + 分类器 + 上下文组装 |
| 导出脚本 | `tools/export_grammar.js` | Obsidian → grammar.js 的机械转换 |
| Agent 引擎 | `assets/askAgent.js` | 有界循环、工具注册表、DeepSeek 调用、错误映射 |
| UI | `index.html` + `app.js` + `styles.css` | 按钮、面板、消息渲染、存笔记 |
| 脚本引入顺序 | index.html：`grammar.js → askAgent.js → app.js` | 与现有 script 标签模式一致 |

---

## 6. 数据设计

```js
// wordbook 卡片新增字段（向后兼容，可选）
card.note: string        // "存为笔记"沉淀内容，卡片详情展示

// localStorage（可选）
'askHistory': 最近一次问答缓存，{ word, messages[] }，切卡/退出即失效
```

会话生命周期：进入复习卡片 → 首次提问创建会话 → 追问追加 → 切卡销毁 → 退出复习全部销毁。

---

## 7. 关键决策记录（ADR）

1. **工具协议用 JSON 决策，不用官方 function calling**：工具全为本地函数，应用执行、模型只做决策；手写协议更稳、可观测、易调试。
2. **免费信息预填**：卡片语境、本地词库、命中语法节直接进首轮 prompt，不占工具轮数；模型唯一需要"调用"的场景极少，典型路径 1 次调用出答案。
3. **语法知识源 = Obsidian 库（RAG-lite）**：按需注入命中章节（≤3 节 + 总览），不全文注入（全库 ~108KB 不可行）；检索为关键词规则（稀疏检索极简版），语料小、问题域窄，无需向量化。
4. **本地词典不作 agent 工具**：模型认知强于扁平词表、精确匹配命中率低；仅保留离线降级用途（见 FR-5）。
5. **例句生成不设独立工具**：作为回答指令的一部分（或用户主动"再给例句"才走第二轮），省一次调用。

---

## 8. 验收标准

### AC-1 主场景四连问（真实用例）

复习卡片原句：
> Bob Liodice, the chief executive of the Association of National Advertisers, says consumers will be worse off if the industry cannot **collect** information about their preferences.

逐条验收：

| # | 提问 | 通过标准 |
|---|---|---|
| 1 | "collect 在这里什么意思？" | 回答给出语境义项"收集、搜集（信息/数据）"+ 画面感 + 同义替换（gather），且引用句中上下文 |
| 2 | "它还有其他意思吗？" | 列出其他义项（收取费用/领取/募集/人群聚集），说明与当前义项的区分 |
| 3 | "母语者怎么理解这个词？" | 用意象解释（主动把散落的东西聚拢），而非罗列词典释义 |
| 4 | "区分 collect 和 receive 的画面差异" | 画面对比（collect 主动去取/聚拢；receive 被动接受送达之物）+ 双方适用场景 + 英中对照例句 |

### AC-2 追问上下文

第 4 问中能引用第 1-3 问的内容（如"刚才说的收集信息"），无需重复提问背景。

### AC-3 成本

- 每问 LLM 调用 ≤ 2 次（可经日志验证）；
- 单次提问输入 ≤ 6K token；
- 同一卡片重复相同问题（命中缓存时）0 次调用。

### AC-4 容错

- 无 API key：提示配置，或离线模板回答，不崩溃；
- 断网/超时：面板不丢已发问题，错误提示明确；
- 401/402：提示文案与现有翻译功能一致。

### AC-5 口径一致

对同一句子的同一语法现象，"问它"回答与长难句分析的术语、判定规则（数谓语/三步判定从句/非谓语 vs 谓语）一致，不互相矛盾。

### AC-6 闭环

"存为笔记"→ 重新打开该卡片详情可见 `note` 内容；复习流程正常结束（评分、FSRS 不受影响）。

---

## 9. 里程碑

| 阶段 | 内容 | 验证方式 |
|---|---|---|
| M1 | 导出脚本 + grammar.js + AskAgent 引擎（含分类器、工具、错误映射） | 命令行/临时页面跑通四连问用例 |
| M2 | UI 接入：按钮、面板、消息渲染、存笔记 | 真机走通 S1-S5 场景 |
| M3 | 打磨：缓存、离线模板、加载态、样式 | 全量过 AC-1 至 AC-6 |

---

## 10. 风险与对策

| 风险 | 对策 |
|---|---|
| token 成本失控 | 硬上限（≤2 轮/问）、免费信息预填、无重试、回答缓存 |
| 回答口径漂移/幻觉 | 语法库按需注入锚定方法论；prompt 内容红线；对照长难句分析口径验收 |
| 知识库与 App 漂移 | Obsidian 为唯一知识源，导出脚本可复跑，产物进 git 可追溯 |
| DeepSeek 不稳定/限流 | 15s 超时、错误码映射、降级路径完整 |
| 复习流被破坏 | 面板不阻塞评分；退出复习强制清理会话；回归 AC-6 |

---

## 附：与现有功能边界

| 功能 | 形态 | 关系 |
|---|---|---|
| 单词/句子翻译（translator.js） | 单轮 JSON | 不动；离线降级复用其本地词库逻辑 |
| 长难句分析（analyzeSentence） | 单轮 JSON | 不动；"问它"语法口径与其保持一致 |
| 本功能（askAgent.js） | 多轮 + 工具 | 新增独立模块，互不耦合 |
